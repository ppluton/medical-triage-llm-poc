{
  "name": "chsa-triage-cloudflare-pages",
  "private": true,
  "scripts": {
    "build": "python3 ../../scripts/build_cloudflare_pages.py --output dist",
    "check:functions": "wrangler pages functions build functions --outdir .wrangler-check --project-directory . --build-output-directory dist",
    "dev": "wrangler pages dev"
  },
  "devDependencies": {
    "wrangler": "4.132.0"
  }
}

# Étape 9 — Configurer les expériences avant de les lancer

Une configuration est une recette versionnée : modèle, données, seed et hyperparamètres. Elle permet de comparer une baseline, le SFT/LoRA puis le DPO sans modifier silencieusement les conditions de l'expérience. Ici, les paramètres non encore testés restent explicitement `proposed`.
